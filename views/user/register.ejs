<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Đăng ký</title>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.1/dist/css/bootstrap.min.css"
    />
    <script src="https://cdn.jsdelivr.net/npm/jquery@3.5.1/dist/jquery.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.1/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>

  <body>
    <%- include('header') %>

    <div class="container" style="padding-top: 10px; margin-bottom: 80px">
      <div class="row justify-content-center">
        <div
          class="row justify-content-center"
          style="
            border: 3px solid #800080;
            margin-top: 50px;
            min-width: 500px;
            padding-bottom: 40px;
            border-radius: 30px;
          "
        >
          <div class="col-md-8">
            <h2 class="my-5" style="text-align: center; color: #800080">Đăng kí</h2>
            <form action="" method="POST" enctype="multipart/form-data">
              <div class="form-group">
                <label for="name" style="color: #800080; font-weight: bold">Họ và tên:</label>
                <input
                  type="text"
                  class="form-control"
                  id="name"
                  placeholder="Nhập tên tài khoản"
                  name="name"
                  style="border: 1px solid #800080"
                  value="<%= name %>"
                />
              </div>
              <div class="form-group">
                <label for="email" style="color: #800080; font-weight: bold">Email:</label>
                <input
                  type="email"
                  class="form-control"
                  id="email"
                  placeholder="Nhập email"
                  name="email"
                  style="border: 1px solid #800080"
                  value="<%= email %>"
                />
              </div>
              <div class="form-group">
                <label for="name" style="color: #800080; font-weight: bold">Số điện thoại:</label>
                <input
                  type="text"
                  maxlength="10"
                  class="form-control"
                  id="phone"
                  placeholder="Số điện thoại"
                  name="phone"
                  style="border: 1px solid #800080"
                  value="<%= phone %>"
                />
              </div>

              <div class="form-group">
                <label for="address" style="color: #800080; font-weight: bold">Địa chỉ:</label>
                <input
                  type="text"
                  class="form-control"
                  id="address"
                  placeholder="Nhập địa chỉ"
                  name="address"
                  style="border: 1px solid #800080"
                  value="<%= address %>"
                />
              </div>
              <div class="form-group">
                <label for="birth" style="color: #800080; font-weight: bold">Ngày sinh:</label>
                <input
                  type="date"
                  class="form-control"
                  id="birth"
                  placeholder="Nhập ngày sinh"
                  name="birth"
                  style="border: 1px solid #800080"
                  value="<%= birth %>"
                />
              </div>

              <div class="form-group">
                <p style="color: #800080; font-weight: bold">Mặt trước CMND</p>
                <input
                  type="file"
                  id="front_IDcard"
                  name="front_IDcard"
                  accept="image/*"
                  required
                />
              </div>

              <div class="form-group">
                <p style="color: #800080; font-weight: bold">Mặt sau CMND</p>
                <input type="file" id="back_IDcard" name="back_IDcard" accept="image/*" required />
              </div>

              <button
                type="submit"
                class="btn btn-success"
                style="
                  width: 100%;
                  background: #800080;
                  margin-bottom: 30px;
                  border-radius: 10px;
                  margin-top: 30px;
                "
              >
                Đăng kí
              </button>

              <%if(error.length > 0){ %>
              <div class="alert alert-<%=type%>"><%=error%></div>
              <%}%>
            </form>
          </div>
        </div>
      </div>
    </div>
    <%- include('footer') %>
  </body>
</html>
