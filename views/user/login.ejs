<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Đăng nhập</title>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.1/dist/css/bootstrap.min.css"
    />
    <script src="https://cdn.jsdelivr.net/npm/jquery@3.5.1/dist/jquery.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.1/dist/js/bootstrap.bundle.min.js"></script>

    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>

  <body>
    <%- include('header') %>

    <div class="container" style="padding-top: 10px; margin-bottom: 80px">
      <div class="row justify-content-center">
        <div
          class="row justify-content-center"
          style="
            border: 3px solid #800080;
            margin-top: 50px;
            min-width: 500px;
            padding-bottom: 40px;
            border-radius: 30px;
          "
        >
          <div class="col-md-8">
            <h2 class="my-5" style="text-align: center; color: #800080">Đăng nhập</h2>
            <form action="" method="POST">
              <div class="form-group">
                <label for="username" style="color: #800080; font-weight: bold">Username:</label>
                <input
                  type="text"
                  class="form-control"
                  id="username"
                  placeholder="Nhập username"
                  name="username"
                  style="border: 1px solid #800080"
                />
              </div>
              <div class="form-group">
                <label for="password" style="color: #800080; font-weight: bold">Password:</label>
                <input
                  type="password"
                  class="form-control"
                  id="password"
                  placeholder="Nhập password"
                  name="password"
                  style="border: 1px solid #800080"
                />
              </div>
              <div style="margin-top: 10px">
                <a style="color: #800080; text-decoration: none" href="/user/forgot"
                  >Quên mật khẩu</a
                >
              </div>
              <div style="margin-top: 10px; margin-bottom: 30px">
                Bạn chưa có tài khoản? <a href="/user/register">Đăng ký</a>
              </div>

              <button
                class="btn btn-primary"
                style="width: 100%; background: #800080; margin-bottom: 30px; border-radius: 10px"
                type="submit"
              >
                Đăng nhập
              </button>
              <%if(error.length > 0){ %>
              <div class="alert alert-<%=type%>"><%= error %></div>
              <%}%>
            </form>
          </div>
        </div>
      </div>
    </div>
    <%- include('footer') %>
  </body>
</html>
