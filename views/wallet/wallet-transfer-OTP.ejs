<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= title %></title>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.1/dist/css/bootstrap.min.css"
    />
    <script src="https://cdn.jsdelivr.net/npm/jquery@3.5.1/dist/jquery.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.1/dist/js/bootstrap.bundle.min.js"></script>

    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <%- include('header') %>

    <section class="service_section layout_padding">
      <div class="container">
        <div class="d-flex flex-column align-items-end">
          <div class="custom_heading-container">
            <hr />
            <h2>Số dư</h2>
          </div>
          <h2 class="balance"><%= wallet.account_balance %></h2>
        </div>
      </div>
      <div
        class="container"
        style="
          max-width: 700px;
          padding: 30px 100px 100px 100px;
          border: 2px solid #800080;
          border-radius: 40px;
          background: #80008044;
        "
      >
        <p style="text-align: center"><img src="/images/s3-blue.png" alt="" /></p>
        <h2 style="text-align: center">Xác thực chuyển tiền</h2>

        <form action="/wallet/transfer-OTP" method="POST">
          <input type="hidden" name="phone" value="<%= phone%>" />
          <input type="hidden" name="money_transfer" value="<%= money_transfer%>" />
          <input type="hidden" name="notes" value="<%= notes%>" />
          <input type="hidden" name="fee" value="<%= fee%>" />
          <input type="hidden" name="person_pay" value="<%= person_pay%>" />

          <div class="form-group" style="margin-top: 20px">
            <label for="otp">Nhập OTP</label>
            <input type="number" name="otp" id="otp" />
          </div>
          <button
            type="submit"
            style="
              width: 100%;
              background: #800080;
              margin-top: 30px;
              border-radius: 10px;
              border: none;
            "
            class="btn btn-primary"
          >
            Xác thưc
          </button>
        </form>
        <form action="/wallet/transfer" method="GET">
          <button
            style="width: 100%; margin-top: 30px; border-radius: 10px; border: none"
            type="submit"
            class="btn btn-danger"
          >
            Hủy
          </button>
        </form>
      </div>
    </section>

    <%- include('footer') %>
  </body>
  <script src="/javascripts/main.js"></script>
</html>
